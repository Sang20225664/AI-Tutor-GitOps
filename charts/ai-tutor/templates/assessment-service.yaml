apiVersion: v1
kind: Service
metadata:
  name: assessment
  namespace: {{ .Values.global.environment }}
  labels:
    {{- include "ai-tutor.labels" . | nindent 4 }}
    app: assessment
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.assessment.port }}
      targetPort: {{ .Values.assessment.port }}
      protocol: TCP
      name: http
  selector:
    app: assessment
